<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.hokkaidoec.mapper.UserMapper">


  <select id="findAll" resultType="com.example.hokkaidoec.entity.User">
    SELECT name, email,address,phone, password,point,totalPurchaseAmount,rankId
    FROM users
    ORDER BY id
  </select>

  <select id="findById" resultType="com.example.hokkaidoec.entity.User">
    SELECT name, email,address,phone, password,point,totalPurchaseAmount,rankId
    FROM users
    WHERE id = #{id}
  </select>

</mapper>